% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/to_ChemoSpec.R
\name{simplerspec_to_ChemoSpec}
\alias{simplerspec_to_ChemoSpec}
\title{{simplerspec} ⚪ ➡️ 🟠 {ChemoSpec}}
\usage{
simplerspec_to_ChemoSpec(
  spc_tbl,
  groups,
  data_column = base::c("spc_pre", "spc_mean", "spc_rs", "spc"),
  freq_column = base::c("xvalues_pre", "wavenumbers_rs", "wavenumbers"),
  name_column = base::c("unique_id", "file_id", "sample_id", "sample_name"),
  .name_column_collapse = NULL,
  .strict_column_check = TRUE,
  ...
)
}
\arguments{
\item{spc_tbl}{\code{\link[simplerspec:gather_spc]{simplerspec::gather_spc}},
\code{\link[simplerspec:resample_spc]{simplerspec::resample_spc}}, \code{\link[simplerspec:average_spc]{simplerspec::average_spc}},
or \code{\link[simplerspec:preprocess_spc]{simplerspec::preprocess_spc}} spectra tibble.}

\item{groups}{Factor with sample class assignments.
Length must match \code{nrow()} of \code{data}.}

\item{data_column}{Column name of the data to use.
Must be one of \verb{"spc_pre", "spc_mean", "spc_rs", "spc"}, or
any column name if \code{strict_column_check} is set to FALSE.}

\item{freq_column}{Column name of the data to use.
Must be one of \verb{"xvalues_pre", "wavenumbers_rs", "wavenumbers"}, or
any column name if \code{strict_column_check} is set to FALSE.}

\item{name_column}{Column name of the data to use.
Must be one of \verb{"unique_id", "file_id", "sample_id", "sample_name"}, or
any metadata column name if \code{strict_column_check} is set to FALSE.}

\item{.name_column_collapse}{(\strong{Default}: NULL) Optional string, that
if set allows multiple metadata column names to be set in \code{name_column},
causing them to be concatenated using \code{.name_column_collapse} as a
separator.
This can be used with \code{.strict_column_check = FALSE} to construct
custom sample identifiers using other metadata such as \code{rep_no}.}

\item{.strict_column_check}{(\strong{Default}: TRUE) Set this to FALSE to allow
using any valid column name on \code{spc_tbl} in \code{data_column} and
\code{freq_column}, and any metadata column names in \code{name_column}.}

\item{...}{
  Arguments passed on to \code{\link[=to_ChemoSpec]{to_ChemoSpec}}
  \describe{
    \item{\code{unit_frequency,unit_intensity}}{\strong{Optional} labels for the x- and y-axis.}
    \item{\code{desc}}{\strong{Optional} description for plots generated with {ChemoSpec}.}
    \item{\code{colors_set,sym_set,alt.sym_set}}{Character vector with group colors,
Numeric vector with group symbol numbers, Character vector with
alternative group symbols.
Their length must match at least the number of levels in \code{groups}.
Groups are assigned colors based on their numeric factor level.
\strong{Defaults to} \code{ChemoSpecUtils::Col8}, \code{ChemoSpecUtils::Sym8} and
\code{base::letters}.
A color-blind friendly palette is available in \code{ChemoSpecUtils::Col7}.
Extended palettes are available at \code{ChemoSpecUtils::Col12},
\code{ChemoSpecUtils::Sym12}, and using other packages, like {RColorBrewer}.}
    \item{\code{.strict_extra_data_names}}{(\strong{Default}: TRUE) Set this to FALSE to allow
columns from \code{extra_data} to overwrite values on the \code{Spectra} object.}
  }}
}
\value{
A \code{ChemoSpec::Spectra()} object.
}
\description{
Some data will not be converted:
\itemize{
\item Spectra columns except the \code{data_column}
\item Wavenumber columns except the \code{freq_column}
}
}
\examples{
data("simplerspec_opus")
simplerspec_to_ChemoSpec(
  simplerspec_opus,
  groups = factor(LETTERS[1:3])
) |> str()

}
\seealso{
\code{to_ChemoSpec()}
}
\keyword{from_simplerspec}
\keyword{to_ChemoSpec}

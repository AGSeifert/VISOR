% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/to_ChemoSpec.R
\name{to_ChemoSpec}
\alias{to_ChemoSpec}
\title{*️⃣ {ChemoSpec}}
\usage{
to_ChemoSpec(
  data,
  groups,
  freq = base::as.numeric(base::colnames(data)),
  names = base::rownames(data),
  desc = "{missing description}",
  unit_frequency = "{missing frequency unit}",
  unit_intensity = "{missing intensity unit}",
  colors_set = ChemoSpecUtils::Col8,
  sym_set = ChemoSpecUtils::Sym8,
  alt.sym_set = base::letters,
  extra_data = NULL,
  .strict_extra_data_names = TRUE
)
}
\arguments{
\item{data}{Numeric matrix with samples in rows and frequencies in cols.}

\item{groups}{Factor with sample class assignments.
Length must match \code{nrow()} of \code{data}.}

\item{freq}{Numeric vector with unique frequencies.
Length must match \code{ncol()} of \code{data}.
\strong{Defaults to} \code{colnames()} of \code{data} cast \code{as.numeric()}.}

\item{names}{Character vector with unique sample names.
Length must match \code{nrow()} of \code{data}.
\strong{Defaults to} \code{rownames()} of \code{data}.}

\item{desc}{\strong{Optional} description for plots generated with {ChemoSpec}.}

\item{unit_frequency, unit_intensity}{\strong{Optional} labels for the x- and y-axis.}

\item{colors_set, sym_set, alt.sym_set}{Character vector with group colors,
Numeric vector with group symbol numbers, Character vector with
alternative group symbols.
Their length must match at least the number of levels in \code{groups}.
Groups are assigned colors based on their numeric factor level.
\strong{Defaults to} \code{ChemoSpecUtils::Col8}, \code{ChemoSpecUtils::Sym8} and
\code{base::letters}.
A color-blind friendly palette is available in \code{ChemoSpecUtils::Col7}.
Extended palettes are available at \code{ChemoSpecUtils::Col12},
\code{ChemoSpecUtils::Sym12}, and using other packages, like {RColorBrewer}.}

\item{extra_data}{\strong{Optional} data frame with samples in rows and additional
per-sample data in its columns. \code{colnames()} of \code{extra_data} is used to
obtain names which will be used as \code{names()} on the \code{Spectra} object.}

\item{.strict_extra_data_names}{(\strong{Default}: TRUE) Set this to FALSE to allow
columns from \code{extra_data} to overwrite values on the \code{Spectra} object.}
}
\value{
A \code{ChemoSpec::Spectra()} object.
}
\description{
*️⃣ {ChemoSpec}
}
\keyword{to_ChemoSpec}
